order_id,
concat(first_name,' ',last_name) as full_name,
city,
State,
order_date,
SUM(quantity) as total_units,
SUM(quantity * list_price) as revenue,
product,
category,
store_name,
CONCAT(first_name,' ', last_name) as sales_rep